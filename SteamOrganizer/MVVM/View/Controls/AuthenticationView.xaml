<Grid x:Class="SteamOrganizer.MVVM.View.Controls.AuthenticationView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:SteamOrganizer.MVVM.View.Controls"
      xmlns:viewModels="clr-namespace:SteamOrganizer.MVVM.ViewModels"
      xmlns:ext="clr-namespace:SteamOrganizer.MVVM.View.Extensions"
      mc:Ignorable="d" 
      Height="Auto" Width="300"
      d:Background="{DynamicResource PrimaryBackgroundBrush}"
      d:DataContext="{d:DesignInstance viewModels:AuthenticationViewModel}">
    <StackPanel>
        <TextBlock HorizontalAlignment="Center"
                   Foreground="#fff"
                   FontWeight="Medium" FontSize="18" >Authentication</TextBlock>
        <Rectangle Fill="{DynamicResource SecondaryBackgroundBrush}" Height="3" Margin="0 10 0 5"/>

        <ext:PasswordBox Height="35" CornerRadius="3"
                         Margin="10" Text="Eldsaes" IsTextHidden="{Binding ElementName=sas,Path=IsChecked}"
                         Background="{DynamicResource TertiaryBackgroundBrush}"/>

        <ToggleButton x:Name="sas" Width="50" Height="30"/>
    </StackPanel>
</Grid>
